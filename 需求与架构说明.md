# 小宠打卡 · 需求与架构说明

> 产品：小朋友电子宠物打卡养成 APP（iPad / 手机 PWA）
> 当前版本：`APP_VERSION = 1.7.0`，数据架构：`SCHEMA_VERSION = 1`
> 最后梳理：2026-08-04

---

## 1. 产品定位

一款面向**小朋友**的"打卡养成"工具：用游戏化的电子宠物，激励孩子完成日常任务；
同时给**家长**提供任务/商品的管理能力。纯前端、可安装到主屏幕、可离线使用，无后端服务器。

## 2. 用户角色

| 角色 | 权限 | 入口 |
|------|------|------|
| 小朋友 | 打卡任务、照顾宠物、商店兑换、查看记录 | 默认直接使用 |
| 家长 | 管理任务/商品（增删改）、进入家长模式、核销兑换 | 设置→家长模式（4位密码） |

## 3. 核心机制：双货币

| 货币 | 图标 | 来源 | 用途 |
|------|------|------|------|
| 积分 ⭐ | 蓝色星星 | 完成任务 | 商店兑换奖品 |
| 金币 🪙 | 黄色硬币 | 完成任务 | 照顾宠物（喂食/洗澡/玩耍/出门） |

两种货币**独立运行**，互不直接兑换，逻辑清晰、便于孩子理解。

## 4. 功能模块

1. **宠物主页**：宠物角色（CSS 绘制，可转身/跳跃/眨眼）、四种状态条（饱腹/清洁/心情/活力）、双货币显示、今日任务进度、连续打卡天数。
2. **任务页**：任务列表、打卡、家长模式下可增删改任务（图标/名称/积分/金币）。
3. **商店页**：分类筛选、商品网格、兑换（消耗积分、扣库存）、家长模式可上架/下架/编辑商品。
4. **我的页**：个人资料、累计统计、兑换记录、待核销商品区（可展开/查看核销二维码）、设置。
5. **家长模式**：4 位数字密码进入；可编辑任务与商品。
6. **首次设置**：首次启动强制设置家长密码（两步确认），存 localStorage。
7. **照顾互动**：喂食（掉落食物+咀嚼动画+音效）、洗澡、玩耍、出门（走动+转身）。
8. **数据备份**：导出/导入 JSON（见第 7 节）。
9. **恢复出厂设置**：设置页"恢复出厂设置"按钮（二次确认），清空全部数据并重回首次设密码流程，等价于把设备还原成刚装好的首次安装状态。

## 5. 数据模型（localStorage）

主数据键：`pet-app-state`（单个 JSON 对象），密码单独存 `parent-password`。

| 字段 | 类型 | 说明 |
|------|------|------|
| `schemaVersion` | number | 数据架构版本，升级兼容用 |
| `pet` | object | name / emoji / level / hunger / clean / mood / energy / lastUpdate |
| `points` | number | 积分余额 |
| `coins` | number | 金币余额 |
| `streak` / `streakDate` | number / string | 连续打卡天数及日期 |
| `tasks[]` | array | {id, name, emoji, points, coins, completed} |
| `products[]` | array | {id, name, emoji, price, stock, category} |
| `exchanges[]` | array | {id, product, emoji, points, date, status: pending/verified} |
| `stats` | object | totalTasks / totalPoints / totalCoins |
| `parentMode` | bool | 当前是否家长模式 |
| `lastTaskDate` | string | 用于每日任务重置 |
| `nextTaskId` / `nextProductId` | number | 自增 ID 计数器 |

## 6. 技术架构

- **纯前端 PWA**：`index.html` + `styles.css` + `app.js` + `manifest.json` + `sw.js`。
- **离线能力**：Service Worker（`sw.js`）缓存核心资源；安装到主屏幕后**断网可用**。
- **缓存强刷**：资源带 `?v=` 查询参数；`sw.js` 的 `CACHE_NAME` 随版本升级（当前 `pet-app-v1.7`）。
  ⚠️ 改完资源务必同步升级两者，否则手机端看不到更新。
- **音效**：Web Audio API 合成，无需音频文件。

## 7. 版本升级与历史数据保留（重点）

数据存在手机浏览器的 **localStorage，按"源地址(origin)"隔离**。保留策略分三层：

### 7.1 同地址升级 = 自动保留
只要部署的**源地址不变**（同一 https 域名 / 同一 localhost），升级 app.js 后：
- localStorage 不变，历史数据自动保留；
- `loadState()` 用默认值合并 + `migrateState()` 迁移，旧数据结构自动补齐新字段。

### 7.2 数据结构升级 = 迁移机制
- 每个数据带 `schemaVersion`；`migrateState(parsed)` 按版本号逐级升级（结构性改动才需写迁移，简单新增字段靠默认合并即可）。
- 升级数据结构时：`SCHEMA_VERSION + 1` 并在 `migrateState` 中追加对应 `if (v < N)` 分支。

### 7.3 换地址 / 换设备 / 防丢失 = 备份导入
若**换部署 URL、清缓存、换手机**，localStorage 不会自动跟随，此时用：
- **导出**："我的 → 设置 → 数据备份 → 导出"，下载一份 JSON（含状态 + 家长密码）。
- **导入**："导入"选择该 JSON，即可完整恢复历史数据到任意设备/地址。

> 结论：日常升级（同地址）→ 无需操作，数据自动在；
> 大变动（换地址/设备）→ 用导出/导入备份迁移，永不丢数据。

### 7.4 一键清空 = 恢复出厂设置
若想在某台已用过的设备上**彻底回到首次安装状态**（换孩子/清测试数据），设置页"恢复出厂设置"按钮会 `removeItem` 掉 `pet-app-state` 与 `parent-password` 后刷新页面：重新走首次设密码流程、状态回到 `DEFAULT_STATE`。操作前有 `confirm` 二次确认，建议先导出备份。

## 8. 已知限制 / 待办

- 兑换页"二维码"为**装饰性图案**（哈希生成，非真实可扫码 QR），核销依赖家长手动确认 + 兑换码文本。
- 删除任务/商品使用浏览器原生 `confirm()`，在 PWA 中样式较朴素。
- 家长密码为 4 位数字、明文存 localStorage（对少儿场景可接受，非高安全需求）。
- 宠物为纯 CSS 绘制，非贴图资源，便于改色与动画但细节有限。
